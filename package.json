{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel-bundler": "^1.12.5"
  },
  "name": "lolpedia",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "parcel build -d build --public-url /LOLPedia/ src/index.html",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
